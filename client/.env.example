# Games API base URL (Rust server in ../server; PORT in server/.env, default 3000).
# Leave empty in dev to use Vite proxy (/games -> http://localhost:3000).
# Set explicitly when the API runs on another host/port, e.g.:
# VITE_API_BASE_URL=http://localhost:3000
